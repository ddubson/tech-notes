# nslookup recipes

Basic lookup

```
nslookup [hostname]
```

Basic lookup using a specific DNS server

```
nslookup [hostname] [dns-server]
```

Specific DNS query

e.g. MX \(Mail\)

```
nslookup -query=mx [hostname]
```



